Name
----
mato (Finnish) - n. worm

Usage
-----
	import interpreter
	interpreter.match_entire(regex, text) → True/False
	interpreter.match_subsection(regex, text) → True/False

Supported features
------------------
	/(.*|text\.)/
